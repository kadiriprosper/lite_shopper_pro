import 'package:flutter/material.dart';

class MyyOrdersPage extends StatelessWidget {
  const MyyOrdersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue,
    );
  }
}